# Critical-path mutation ratchet. A newly missed or timed-out mutant is a
# regression. Broaden `.cargo/mutants.toml` only together with tests that keep
# these values from increasing.
MAX_MISSED_MUTANTS=0
MAX_TIMEOUT_MUTANTS=0
