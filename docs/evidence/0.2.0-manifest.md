# Release evidence manifest (0.2.0 prep)

Gate-run capture (UTC **2026-08-02T06:49:56Z**):

- **git HEAD:** `5131e4db (tip; feature payload c55dd68980e43c82b9e397f22c5ae569beff6b6a)`
  (`feat: 0.2 dual-lane soundness API with private unchecked cores`)
- **worktree:** clean for package paths (only untracked `package-lock.json`,
  not part of the crate)
- **rustc:** `rustc 1.95.0 (59807616e 2026-04-14)`
- **scratch:** implementer `{SCRATCH}/release-gates/SUMMARY.txt`,
  `publish-clean-tree.log`, conformance/budgets gap notes

| Claim | Artifact | Command | Status |
|-------|----------|---------|--------|
| Safe constructors / no public raw UB | `sbe/tests/hft_001_soundness_test.rs` | `cargo test -p ergo-sbe --test hft_001_soundness_test` | green (11 tests; message + group/entry) |
| Group/entry zero-check privacy | codegen + golden | private `unsafe fn wrap_trusted` / entry `wrap` | green |
| `start_entry` capacity pre-check | `group_encoder` + hft_001 | `BufferTooShort` before pos/written advance | green |
| Hostile short buffers | baseline + hostile + hft_001 | via product tests | green |
| Null sentinel LE/BE widths | `codegen::nullification` | via product tests | green |
| sbe-tool wire parity | `sbe_tool_wire_parity_test` | via `just test` | green |
| Multi-schema wire parity | multi-schema parity | via `just test` | green |
| Domain fallible paths | `try_*` / `try_from_decoder` | samples + domain tests | green |
| Compatibility profile | `docs/SBE_COMPATIBILITY.md` | present | green |
| Migration guide | `docs/MIGRATION_0_1_TO_0_2.md` | present | green |
| HFT-008 `_unchecked` keep | `unchecked-keep-manifest.json` | 10× release Instant multi-run | **keep=false all** (private cores) |
| Typestate + source-size (HFT-006) | `hft_006_typestate_budgets_test` | cargo test | green |
| Profiles HftLean/Full (HFT-009) | `hft_009_profile_test` | cargo test | green |
| Stale 0.1 interface reject | `hft_stale_interface_test` | cargo test | green |
| Warning-free consumer | `hft_005_warning_free_consumer_test` | cargo test | green |
| Format / clippy / full suite | `just test` | SCRATCH `just-test.log` | green |
| Supply chain | `cargo deny check` + `cargo audit` | SCRATCH logs | green |
| Rustdoc `-D warnings` | sbe + cluster | SCRATCH `rustdoc-*.log` | green |
| Book | `just book-ci` | SCRATCH `book-ci.log` | green |
| Publish dry-run (**clean tree**) | `cargo publish -p ergo-sbe --dry-run` | SCRATCH `publish-clean-tree.log` EXIT 0 | **green** (Packaged 41 files, verify compile, abort upload) |
| Monomorphization-count budget | — | not automated | **DEFERRED** (`{SCRATCH}/budgets/monomorphization.txt`) |
| FIX SBE Conformance | external Java suite | Java 17 + SBE submodule present; suite **not run** | **GAP** (`{SCRATCH}/conformance/GAP.txt`) |
| crates.io publish / git tag | — | — | **not done** (needs explicit user authority) |

## Supply-chain notes (HFT-005)

- `crossbeam-epoch` ≥0.9.20 (RUSTSEC-2026-0204).
- `iai-callgrind` removed (RUSTSEC-2026-0173 via `proc-macro-error2`).

## HFT-001 / HFT-008

- Message and group entry zero-check constructors are **module-private**
  `unsafe fn` (not public safe).
- Public `_unchecked` constructor twins: **all keep=false** (see
  `unchecked-keep-manifest.json`).

## What still blocks claiming “0.2.0 released”

1. User-authorized **crates.io publish** + **git tag** (dry-run only so far).
2. Optional follow-ups: FIX SBE Conformance when harness is wired; monomorphization-count
   harness if required for full HFT-006 text.

## Capture

```text
git rev-parse HEAD   # c55dd68980e43c82b9e397f22c5ae569beff6b6a
rustc -Vv
cargo publish -p ergo-sbe --dry-run   # EXIT 0 on clean tree
cat {SCRATCH}/release-gates/publish-clean-tree.log
cat {SCRATCH}/release-gates/SUMMARY.txt
```
