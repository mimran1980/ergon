# Release evidence manifest (0.2.0 prep)

Gate-run capture (UTC **2026-08-02T06:33:25Z**):

- **git HEAD:** `979876f40c5aa831f195fdf9fbc65156bebbf518`
- **worktree:** dirty (18 `sbe/` paths + broader dual-lane/sample/docs WIP) —
  clean-tree `cargo publish` refuses without `--allow-dirty`
- **rustc:** `rustc 1.95.0 (59807616e 2026-04-14)`
- **scratch:** implementer `{SCRATCH}/release-gates/SUMMARY.txt` and per-gate logs

| Claim | Artifact | Command | Status |
|-------|----------|---------|--------|
| Safe constructors / no public raw UB | `sbe/tests/hft_001_soundness_test.rs` | `cargo test -p ergo-sbe --test hft_001_soundness_test` | green |
| Hostile short buffers | baseline + hostile | via `just test` | green |
| Null sentinel LE/BE widths | `codegen::nullification` | via product tests | green |
| sbe-tool wire parity | `sbe_tool_wire_parity_test` | via `just test` | green |
| Multi-schema wire parity | multi-schema parity | via `just test` | green |
| Domain fallible paths | `try_*` / `try_from_decoder` | samples + domain tests | green |
| Compatibility profile | `docs/SBE_COMPATIBILITY.md` | present | green |
| Migration guide | `docs/MIGRATION_0_1_TO_0_2.md` | present | green |
| HFT-008 `_unchecked` keep | `unchecked-keep-manifest.json` + multi-run | 10× release Instant | **keep=false all** (private cores) |
| Typestate + source-size (HFT-006) | `hft_006_typestate_budgets_test` | `cargo test …hft_006…` | green |
| Profiles HftLean/Full (HFT-009) | `hft_009_profile_test` | `cargo test …hft_009…` | green |
| Stale 0.1 interface reject | `hft_stale_interface_test` | cargo test | green |
| Warning-free consumer | `hft_005_warning_free_consumer_test` | cargo test | green |
| Format | `cargo fmt --all --check` | just test 1/7 | green |
| Supply chain | `cargo deny check` + `cargo audit` | SCRATCH `deny-check.log` / `audit.log` | green (EXIT 0) |
| Rustdoc `-D warnings` | sbe + cluster | SCRATCH `rustdoc-*.log` | green |
| Book | `just book-ci` | SCRATCH `book-ci.log` | green |
| Full product + samples + Aeron HA | `just test` | SCRATCH `just-test.log` | green |
| Publish dry-run (**dirty**) | `cargo publish -p ergo-sbe --dry-run --allow-dirty` | Packaged 41 files, verify compile | green |
| Publish dry-run (**clean tree**) | `cargo publish -p ergo-sbe --dry-run` | no `--allow-dirty` | **BLOCKED** — dirty worktree |
| Monomorphization-count budget | — | not automated | **DEFERRED** (`{SCRATCH}/budgets/monomorphization.txt`) |
| FIX SBE Conformance | external Java suite | — | **GAP** (`{SCRATCH}/conformance/`) |
| crates.io publish / git tag | — | — | **not done** (needs clean commit + explicit user authority) |

## Supply-chain notes (HFT-005)

- `crossbeam-epoch` ≥0.9.20 (RUSTSEC-2026-0204).
- `iai-callgrind` removed (RUSTSEC-2026-0173 via `proc-macro-error2`).

## HFT-008

Public zero-check twins are **not** shipped. Generated cores are
module-private `unsafe fn *_unchecked`. See
`docs/evidence/unchecked-keep-manifest.json` (release multi-run: exact
`wrap_and_apply_header` ratio ≈1.00).

## What still blocks claiming “0.2.0 released”

1. **Commit** the WIP tree so clean-tree `cargo publish --dry-run` passes without
   `--allow-dirty`.
2. User-authorized **crates.io publish** + **git tag** (not done here).
3. Optional follow-ups (not ship-blockers for dry-run prep): monomorphization-count
   harness; FIX SBE Conformance when Java env available; keep=true re-measure if
   a future public twin is proposed.

## Capture

```text
git rev-parse HEAD
rustc -Vv
date -u
ls {SCRATCH}/release-gates/
cat {SCRATCH}/release-gates/SUMMARY.txt
```
