//! Cluster SBE codecs — ergo-sbe-generated from Aeron schemas (OUT_DIR).
//!
//! - `session` — Aeron cluster session schema 111
//! - `mark`    — cluster mark schema
//!
//! The generated codec surface is deliberately **unstable** and hidden from
//! docs.rs via `#[doc(hidden)]` on the re-export in `lib.rs`. Normal
//! applications should use the high-level `AeronCluster` API. This module
//! exists for repository tests and low-level protocol experimentation.
//!
//! # Lint scope
//!
//! `missing_docs`, `unsafe_code`, and `clippy` are allowed here — the
//! generated codec is not consumer-facing API. The supported, documented
//! surface is in the parent crate's manual modules.
#![allow(unused_unsafe, missing_docs, unsafe_code)]
//! Generated by `build.rs` (`generate_to_out_dir`) at compile time. The
//! sbe-tool reference runtime for benchmarks lives at
//! `cluster/benches/reference_sbe/` (Criterion-private).

ergo_sbe::sbe_mod!(pub session);
ergo_sbe::sbe_mod!(pub mark);

#[cfg(test)]
mod tests;
