# Cluster Tutorial

Connect, offer, poll, keep-alive, close walkthrough.

Uses `ergo-aeron-cluster`. Requires Java 17+ and built Aeron artifacts.

```sh
just build-aeron-jars
cargo run --manifest-path samples/cluster-tutorial/Cargo.toml
```
