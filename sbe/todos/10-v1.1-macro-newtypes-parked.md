⚠️ **DEFERRED — post-v1.** Annotation macro + semantic newtypes is a planned feature for after the initial release. This todo tracks design intent, not current implementation work.

---

# v1.1: annotation macro + semantic newtypes + parked

**Blocked by:** `09-ergosbe-build-driver`

`#[ergosbe::schema("car.xml")] mod messages;` proc-macro front-end. Semantic
newtypes for trading domains. `no_std`, serde (parked per DECISIONS.md).

## Acceptance criteria

- [ ] `ergosbe-macros` crate: `#[schema("path.xml")]` attribute macro
- [ ] Semantic newtypes: Price, Qty, UTCTimestamp, LocalMktDate, SecurityID
- [ ] `semantic-newtypes` config flag, opt-in
- [ ] (Parked) `no_std` + `alloc` feature split
- [ ] (Parked) `Serialize`/`Deserialize` on decoder views
- [ ] (Parked) Opt-in helpers: `finish()`/`validate()`, `reset_count_to_index()`,
  `copy_to_slice`/`write_to`

Ref: `design/DECISIONS.md` §11 slice 13, §4, §9, §11 parked.
