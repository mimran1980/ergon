[package]
name = "l3-book"
version = "0.1.0"
edition = "2024"
publish = false
description = "L3 order book sample — nested groups encode/decode with dummy data"

[dependencies]
ergo-sbe = { path = "../../sbe" }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[build-dependencies]
ergo-sbe = { path = "../../sbe" }

[[bench]]
name = "api_combinations"
harness = false
