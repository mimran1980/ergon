# The image build context is this directory; keep it to what the Dockerfiles
# actually COPY (release binaries plus the recorder app sources).
target/*
!target/release/
!target/release/ingester
!target/release/archive-agent
.venv/
artifacts/
fixtures/
notebooks/
observability/
deploy/kind*.yaml
