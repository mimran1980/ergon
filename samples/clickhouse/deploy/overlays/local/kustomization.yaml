apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
# Local fixture profile: deterministic replay, no external network after
# images are built. Sizes above are the tested minimum profile.
